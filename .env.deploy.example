# ChoirLoop Deployment Configuration
# Copy this file to .env.deploy and fill in your FTP credentials
# .env.deploy is excluded from git for security

# FTP Server Configuration
FTP_HOST=ftp.yourserver.com
FTP_USER=your_ftp_username
FTP_PASS=your_ftp_password
FTP_REMOTE_DIR=/httpdocs

# Production URLs (used for verification messages)
PROD_URL=https://choirloop.rosakehlchen.de
